blog:
  admin:
    username: test-admin
    password: test-password-only

spring:
  datasource:
    url: ${BLOG_TEST_DB_URL:jdbc:mysql://localhost:3306/blog}
    username: ${BLOG_TEST_DB_USERNAME:bloguser}
    password: ${BLOG_TEST_DB_PASSWORD}
